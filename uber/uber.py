from objects.uberDev import vehicleDetails, appLaunch, driverLocation

def main():
    # appData = appLaunch()
    # driverTasks = appData['driverTasks']
    # print(f'ONLINE: {driverTasks['driverState']['online']}  TIME: {driverTasks['meta']['lastModifiedTimeMs']}')
    # item_text = 'VEHICLE'
    # print('='*53 + f'\nSR# {item_text.center(20)}PLATE\t\tACCESS\tUSING\n' + '='*53)
    # vehicles = vehicleDetails()
    # for vehicle in vehicles:
    #     car = vehicle['make'] + ' ' + vehicle['model']
    #     print(f'{vehicles.index(vehicle):<1}   {car:20}{vehicle['licensePlate']}\t\t{vehicle['isAccessible']}\t{vehicle['isDefault']}')

    # response = driverLocation()
    pass

if __name__ == "__main__":
    main()
